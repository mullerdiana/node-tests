/* eslint-disable max-len */
// Tipo específico de alidação, checagem ou verificação. As asserções poderão ser verdadeiras ou falsas.
if (2 === 2) // true
{
  if (15 > 42) // false
  {
    if (2 >= 2) // true

    { ['abacaxi', 'banana'].includes('banana'); }
  }
} // true
